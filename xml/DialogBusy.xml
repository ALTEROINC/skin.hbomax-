<?xml version="1.0" encoding="UTF-8"?>
<window>
  <controls>
    <!-- Minimal top-right busy indicator — replaces Estuary's centered dim+panel
         "Working..." dialog so background operations (e.g. trailer resolution)
         don't visibly interrupt whatever's on screen. -->
    <control type="image">
      <left>1856</left>
      <top>24</top>
      <width>40</width>
      <height>40</height>
      <texture>special://skin/media/overlays/hero_trailer_spinner.gif</texture>
    </control>
  </controls>
</window>
