name: Deploy Kodi Repository

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Get version from addon.xml
        id: version
        run: |
          VERSION=$(grep -oP 'version="\K[^"]+' addon.xml | head -1)
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Detected version: $VERSION"

      - name: Build Kodi zip
        run: |
          VERSION=${{ steps.version.outputs.version }}
          mkdir -p site/repo/skin.hbomax.dev
          rsync -a \
            --exclude='.git' \
            --exclude='.github' \
            --exclude='build' \
            --exclude='tmp' \
            --exclude='backup' \
            --exclude='.claude' \
            --exclude='.DS_Store' \
            --exclude='.tmp_generalsans' \
            --exclude='.tmp_resvg' \
            --exclude='site' \
            . site/repo/skin.hbomax.dev/
          cd site/repo
          zip -r skin.hbomax.dev/skin.hbomax.dev-${VERSION}.zip skin.hbomax.dev/ \
            --exclude="skin.hbomax.dev/*.zip"
          cp ../../addon.xml skin.hbomax.dev/addon.xml
          echo "Built skin.hbomax.dev-${VERSION}.zip"

      - name: Generate addons.xml and checksum
        run: python3 .github/scripts/generate_repo.py

      - name: Assemble site
        run: |
          touch site/.nojekyll
          cp .github/scripts/index.html site/index.html
          echo "Site contents:"
          find site -type f

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
